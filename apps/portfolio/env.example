# WebSocket URL for MC Server Stats
# This is the endpoint where the MC plugin pushes server stats via WebSocket
VITE_WS_SERVER_STATS_URL=wss://discord-ai-bot-production-d077.up.railway.app/ws/server-stats

# Optional: API key if server-stats endpoint requires auth
# VITE_SERVER_STATS_API_KEY=your-api-key-here
