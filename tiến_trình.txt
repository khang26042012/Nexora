================================================================
  TIẾN TRÌNH LÀM VIỆC — NEXORAGARDEN
  Ghi lại mọi thứ đã làm được để AI khác hiểu context
  Cập nhật: [session 42] FEAT — Chatbot: thinking mode ẩn tokens, hiện ⚙️ Đang suy nghĩ...
================================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[session 42] FEAT — Chatbot: thinking mode ẩn tokens, hiện ⚙️ Đang suy nghĩ...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Model: gemma-4-26b-a4b-it (hỗ trợ thinking qua thinking_config runtime)

Backend (chat.ts):
  - needsThinking(): phát hiện câu phức tạp hoặc chứa keyword "thinking":
    + Keyword: "thinking", "viết code", "lập trình", "thuật toán", "debug", "phân tích sâu"...
    + Hoặc message > 300 ký tự
  - Nếu cần thinking: gửi thinking_budget=8192, ngược lại: 0 (không thinking)
  - Stream filtering: lọc bỏ các chunk có thought=true (thinking tokens) — không forward
  - Gửi SSE event đặc biệt: {"type":"thinking","active":true/false} cho frontend

Frontend (Chat.tsx):
  - Thêm state isThinking
  - callGeminiStream xử lý event type:"thinking" → cập nhật setIsThinking
  - UI streaming bubble:
    + isThinking && !streamText → ⚙️ Đang suy nghĩ... (pulsing)
    + streamText → hiện text + cursor nhấp nháy
    + fallback → 3 dots bouncing
  - finally: reset isThinking=false

Files thay đổi:
  - packages/api-server/src/routes/chat.ts
  - apps/portfolio/src/pages/Chat.tsx

Push GitHub: commit b0fe3228 ✅
Restart: API Server ✅ | Start application ✅
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[session 41] FIX — Chatbot đổi model sang gemma-4-26b-a4b-it
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - gemini-1.5-flash không còn hỗ trợ cho API v1beta → chatbot báo lỗi "not found"

Đã làm:
  - Đổi GEMINI_MODEL trong packages/api-server/src/routes/chat.ts
    từ: "gemini-1.5-flash"
    sang: "gemma-4-26b-a4b-it"
  - Restart "packages/api-server: API Server" → RUNNING ✅
  - Push GitHub: commit a22e06ba (Tree API, 1 file) ✅

Files thay đổi:
  - packages/api-server/src/routes/chat.ts

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[session 40] MIGRATION — Replit environment mới (lần 12)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → OK (packages đã up to date, 2.4s)
  2. better-sqlite3 build từ source:
     - prebuild-install: không có binary cho Node 24.13.0
     - Dùng node-gyp với nodedir=/nix/store/.../nodejs-24.13.0 → build thành công ✅
     - better_sqlite3.node + test_extension.node tại build/Release/ ✅
  3. Restart "Start application" (portfolio, port 21113) → RUNNING ✅
  4. Restart "packages/api-server: API Server" (port 8080) → RUNNING ✅
  5. Gửi Telegram thông báo hoàn tất ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - SQLite DB initialized ✅
  - ffmpeg: bundled vào dist/ffmpeg ✅
  - yt-dlp: downloaded & ready ✅

WARN (không phải lỗi):
  - TELEGRAM_TOKEN not set → Bot Telegram tắt (cần set trong Secrets)
  - NexoraGarden dist not found → bình thường dev mode
  - Portfolio dist not found → bình thường dev mode
  - attached_assets/* workflows: NOT_STARTED → bình thường (file tham khảo cũ)

Note kỹ thuật (dùng cho migration sau):
  - Node 24.13.0 không có prebuild binary cho better-sqlite3
  - Từ session 40: ĐÃ CÓ setup-sqlite.mjs tự động → không cần build thủ công nữa
  - Binary prebuilt lưu tại: packages/api-server/prebuilt/better_sqlite3.node (đã push GitHub)
  - Script tự copy binary → nếu incompatible mới fallback build từ source
  - Dev command tự chạy script trước: "node ./setup-sqlite.mjs && pnpm run build && ..."

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[session 39] FEAT — Thêm tool mới: Prompt Builder
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  Backend:
  - Tạo packages/api-server/src/routes/promptgen.ts
  - POST /api/prompt-gen: nhận { mode, lang, ...fields } → stream Gemini 2.5 Flash
  - mode "manual": nhận role/task/context/tone/outputFormat/extra → AI ghép thành prompt chuẩn
  - mode "ai": nhận description đơn giản → AI viết lại thành prompt hoàn chỉnh
  - Đăng ký vào routes/index.ts

  Frontend:
  - Tạo apps/portfolio/src/pages/tools/PromptBuilder.tsx
  - Hai mode chính: "Tự set" (form fields) và "AI viết" (textarea mô tả)
  - "Tự set": 5 field (Đóng vai, Nhiệm vụ, Ngữ cảnh, Phong cách pills, Định dạng pills, Yêu cầu thêm)
  - "AI viết": textarea mô tả ngắn → AI viết lại prompt đầy đủ (không đóng vai)
  - Language toggle: 🇻🇳 VI / 🇺🇸 EN (ảnh hưởng ngôn ngữ output prompt)
  - Output: streaming, cursor nhấp nháy, nút Copy, tip dán vào ChatGPT/Claude/Gemini
  - Style chuẩn với các tool khác (AnimBorderCard, Particles, running-border)
  - Đăng ký route /tool/prompt-builder trong App.tsx
  - Thêm card "Prompt Builder" (icon Wand2, tag AI) lên đầu danh sách Tool.tsx

Files thay đổi:
  - packages/api-server/src/routes/promptgen.ts (MỚI)
  - packages/api-server/src/routes/index.ts
  - apps/portfolio/src/pages/tools/PromptBuilder.tsx (MỚI)
  - apps/portfolio/src/App.tsx
  - apps/portfolio/src/pages/Tool.tsx

Push GitHub: commit c19156ae ✅
Portfolio: RUNNING ✅ | API Server: RUNNING ✅
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[session 38c] MIGRATION — Replit environment mới (lần 11)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → OK (46.7s)
  2. better-sqlite3 build từ source (npm run build-release trong pnpm store) → better_sqlite3.node ✅
  3. Restart "Start application" (portfolio) → RUNNING ✅
  4. Restart "packages/api-server: API Server" → RUNNING ✅
  5. Gửi Telegram thông báo hoàn tất ✅

Kết quả:
  - Portfolio: RUNNING ✅
  - API Server: RUNNING ✅
  - SQLite DB initialized ✅
  - ffmpeg: bundled vào dist/ffmpeg ✅
  - yt-dlp: ready ✅

WARN (không phải lỗi):
  - attached_assets/* workflows: NOT_STARTED → bình thường (file tham khảo cũ)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[11/04/2026 session 37] MIGRATION — Replit environment mới (lần 10)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → 740 packages OK
  2. better-sqlite3 build từ source (npm run build-release) → Release/better_sqlite3.node ✅
  3. Restart "Start application" (portfolio, port 21113) → RUNNING ✅
  4. Restart "packages/api-server: API Server" → RUNNING ✅
  5. Gửi Telegram thông báo hoàn tất ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - SQLite DB initialized ✅
  - ffmpeg: bundled vào dist/ffmpeg ✅
  - yt-dlp: ready ✅

WARN (không phải lỗi):
  - attached_assets/* workflows: NOT_STARTED → bình thường (file tham khảo cũ)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[14/04/2026 session 38b] DATA — Cập nhật NexoraData.txt cho AI biết về Image to Text
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã cập nhật NexoraData.txt (v1.2):
  - DỰ ÁN 3 NexoraTool: thêm "4. Image to Text" vào danh sách tool con
  - Phần 6: Thêm TOOL 4 — IMAGE TO TEXT / OCR (cách dùng, kỹ thuật, edit mode, giới hạn)
  - Chat Bot đổi thành TOOL 5
  - Phần 7 Monorepo: thêm ImageToText.tsx vào cấu trúc thư mục
  - Phần 9 Q&A: thêm Q&A "Image to Text là gì?", cập nhật "NexoraTool có giới hạn gì?"
  - Phần 10 API: thêm endpoint POST /api/ocr
  - Footer: cập nhật ngày 14/04/2026, phiên bản 1.2

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[14/04/2026 session 38] FEAT — Thêm tool mới: Image to Text (OCR)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  Backend:
  - Tạo packages/api-server/src/routes/ocr.ts
  - POST /api/ocr: nhận { content (base64), mimeType } → stream GLM-4.6V-Flash đọc ảnh
  - System prompt OCR: marker chuẩn [C]...[/C], **bold**, -, ---, giữ nguyên cấu trúc tài liệu
  - Đăng ký vào routes/index.ts

  Frontend:
  - Tạo apps/portfolio/src/pages/tools/ImageToText.tsx
  - Upload ảnh: drag & drop hoặc click (JPG/PNG/WEBP/GIF/BMP, tối đa 10MB)
  - Preview thumbnail sau khi chọn ảnh
  - Tag gợi ý: ảnh tài liệu, scan sách, bảng trắng, chữ viết tay, đề thi
  - Stream kết quả OCR → RenderOutput chuẩn marker
  - Edit mode: bút chì / mắt, textarea monospace, sync rawResult
  - 5 định dạng tải về: txt, md, html, json, docx (dùng lại hệ thống TextFormatter)
  - Copy button với animation Đã copy
  - Đăng ký route /tool/image-to-text trong App.tsx
  - Thêm card "Image to Text" (icon ScanText, tag OCR) lên đầu danh sách Tool.tsx

Files thay đổi:
  - packages/api-server/src/routes/ocr.ts (MỚI)
  - packages/api-server/src/routes/index.ts
  - apps/portfolio/src/pages/tools/ImageToText.tsx (MỚI)
  - apps/portfolio/src/App.tsx
  - apps/portfolio/src/pages/Tool.tsx

Push GitHub: commit e88856e0 ✅
Portfolio: RUNNING (port 21113) ✅ | API Server: RUNNING (port 8080) ✅
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[11/04/2026 session 37c] FEAT — Text Formatter: thêm tính năng chỉnh sửa kết quả AI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  - Thêm nút "Chỉnh sửa" / "Xem trước" (icon Pencil/Eye) trong header kết quả
  - Khi bật Chỉnh sửa: vùng output chuyển sang textarea monospace, có thể gõ tự do
  - Khi tắt: trở về RenderOutput (preview đẹp)
  - Mọi thay đổi khi edit tức thì sync vào rawResult → Copy / Tải về dùng nội dung đã sửa
  - Viền chạy đổi màu sáng hơn khi đang edit (visual indicator)
  - Hint text ở dưới: "Đang chỉnh sửa — Copy / Tải về sẽ dùng nội dung đã sửa"
  - Submit mới → tự thoát edit mode, reset sạch
  - Áp dụng cho cả 3 chế độ: Upload File, Nhập text, Tạo bằng AI

Files thay đổi:
  - apps/portfolio/src/pages/tools/TextFormatter.tsx

Push GitHub: commit 53057abd ✅
Portfolio: RUNNING (port 21113) ✅
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[11/04/2026 session 37b] FIX — Text Formatter AI "Tạo bằng AI" không còn ngáo về Nexora
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - Tool "Tạo bằng AI" trong Text Formatter gọi GLM-4.6V-Flash qua api.llm7.io
  - GENERATE_SYSTEM_PROMPT không có context về Nexora/Khang
  - Khi gõ "Tạo đề thi về Nexora và Khang" → AI bịa Nexora là thuốc kháng sinh / công ty dược

Fix lần 1 (commit ecd860e6):
  - Thêm NEXORA_CONTEXT vào GENERATE_SYSTEM_PROMPT
  - AI biết Nexora là thương hiệu của Khang

Fix lần 2 (commit 1027426f) — vẫn bị lỗi: model copy-paste placeholder vào output:
  - Vấn đề: format rules viết "**I. Tên**" → model in literal "**I. Tên**" vào kết quả
  - Fix: viết lại toàn bộ GENERATE_SYSTEM_PROMPT:
    + Dùng ví dụ thực tế thay placeholder (VD: **I. Thông tin cá nhân**)
    + Thêm mục NGHIÊM CẤM: không viết placeholder, không lặp context
    + Thay "QUY TẮC ĐỊNH DẠNG" → "QUY TẮC OUTPUT" rõ ràng hơn

Files thay đổi:
  - packages/api-server/src/routes/formatter.ts (viết lại GENERATE_SYSTEM_PROMPT)

Restart: "packages/api-server: API Server" → RUNNING ✅
Push GitHub: 2 commits (Tree API) ✅
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[11/04/2026 session 36] UPDATE — Thay data training NexoraAI bằng NexoraData.txt mới
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - Session 35 dùng data_nexorax_garden.txt (file kỹ thuật NexoraGarden) cho AI context
  - User yêu cầu dùng NexoraData.txt (file huấn luyện AI chính thức)

Đã làm:
  1. Upload NexoraData.txt (nexora_ai_training_(1)_1775869923423.txt) vào project
     - Copy vào apps/portfolio/src/lib/NexoraData.txt (525 dòng)
     - Gồm 11 phần: thông tin cá nhân, liên hệ, kỹ năng, hành trình, dự án,
       tool details, kiến trúc monorepo, design system, Q&A training, API info, chú ý
  2. Cập nhật apps/portfolio/src/lib/nexoraData.ts
     - Từ: embed thủ công nội dung data_nexorax_garden.txt
     - Sang: import NexoraData.txt?raw (Vite raw import, gọn và chính xác)
  3. Push GitHub commit

Trạng thái:
  - Portfolio: RUNNING port 21113 ✅
  - API Server: RUNNING port 8080 ✅
  - NexoraAI Chat: dùng đúng file training mới ✅
  - ESP32 firmware: chưa thay đổi (chỉ làm khi có yêu cầu cụ thể)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 35] FEAT — NexoraAI Chat thật (Gemini 2.5 Flash + file/ảnh)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. Tạo apps/portfolio/src/lib/nexoraData.ts:
     - Embed toàn bộ data_nexorax_garden.txt (~50KB) làm system context
     - Auto-generated bằng script node, JSON.stringify safe
  2. Viết lại apps/portfolio/src/pages/Chat.tsx:
     - Gọi Gemini 2.5 Flash REST API trực tiếp từ frontend (không cần backend)
     - Streaming response (SSE) → chữ hiện dần dần, có cursor nhấp nháy
     - Multi-turn: duy trì history[] trong state → AI nhớ context trò chuyện
     - File/ảnh upload qua FileReader → base64 → Gemini inlineData
       * Ảnh: hiện thumbnail trong chip + bubble preview
       * File khác (PDF, video, audio, text...): hiện icon + tên file
       * Giới hạn 15MB/file (Gemini inline limit)
     - Error handling: hiển thị lỗi API ngay trong UI + error bubble
     - Loading state: spinner trong nút gửi khi đang xử lý
     - Disable input khi đang loading
     - Suggested prompts cập nhật thêm 1 câu về hệ thống
  3. Push GitHub commit 35f045bc (Tree API, 1 commit)
  4. Restart "Start application" → RUNNING ✅

Files thay đổi:
  - apps/portfolio/src/lib/nexoraData.ts (tạo mới)
  - apps/portfolio/src/pages/Chat.tsx (viết lại)

GitHub commit: 35f045bc
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 34] MIGRATION — Replit environment mới (lần 9)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → OK
  2. better-sqlite3 rebuild từ source (npm run build-release trong node_modules) → better_sqlite3.node ✅
  3. Thêm workflow "packages/api-server: API Server" (PORT=8080) → RUNNING ✅
  4. Restart "Start application" (portfolio, port 21113) → RUNNING ✅
  5. Gửi Telegram thông báo hoàn tất ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - SQLite DB initialized ✅
  - ffmpeg: bundled vào dist/ffmpeg ✅
  - yt-dlp: ready ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt (cần set trong Secrets)

WARN (không phải lỗi):
  - attached_assets/* workflows: FAILED → bình thường (chỉ là file tham khảo cũ)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 33] FEAT — Bundle ffmpeg vào dist/ lúc build (không chờ download)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - Render/Railway không có native ffmpeg → server phải download ~40MB lúc khởi động
  - Trong khi đó, trim/yt-downloader bị giới hạn combined format thôi

Đã làm:
  1. build.mjs: Thêm bước downloadFfmpeg() sau esbuild:
     - Nếu host có native ffmpeg (Replit/Linux) → copy vào dist/ffmpeg
     - Nếu không có → download static binary từ GitHub, giải nén vào dist/ffmpeg
     - dist/ffmpeg được copy cùng server bundle → sẵn sàng ngay khi start
  2. youtube.ts: findNativeFfmpeg() ưu tiên path.join(__dirname, 'ffmpeg') trước
     → tức là dist/ffmpeg được check đầu tiên trước native/PATH
  3. trim.ts: findNative() tương tự, ưu tiên dist/ffmpeg
  4. Cả 2 file: thêm declare const __dirname (ESM compat)

Kết quả:
  - Trên Replit: [build] ffmpeg bundled → dist/ffmpeg ✅
  - Server log: [ffmpeg] native found: .../dist/ffmpeg ✅
  - trim-ffmpeg: native: .../dist/ffmpeg ✅
  - Cắt video / tải HD ngay lập tức, không chờ download gì cả

Files thay đổi:
  - packages/api-server/build.mjs
  - packages/api-server/src/routes/youtube.ts
  - packages/api-server/src/routes/trim.ts

GitHub commit: 4fee0e39
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 32] MIGRATION — Replit environment mới (lần 8)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → 740 packages OK
  2. pnpm rebuild better-sqlite3 → build/Release/better_sqlite3.node ✅
  3. Restart "apps/portfolio: web" (port 21113) → RUNNING ✅
  4. Restart "packages/api-server: API Server" (port 8080) → RUNNING ✅
  5. Gửi Telegram thông báo hoàn tất ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - SQLite DB initialized ✅
  - WebSocket /ws + /ws-browser: sẵn sàng ✅
  - ffmpeg native: /nix/store/.../bin/ffmpeg ✅
  - yt-dlp: ready ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt

WARN (không phải lỗi):
  - NexoraGarden dist not found → bình thường dev mode
  - Portfolio dist not found → bình thường dev mode
  - attached_assets/* workflows: NOT_STARTED → bình thường (file tham khảo cũ)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 31] FEAT — Thêm lại avatar animation + scroll reveal 2 chiều
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. Thêm lại avatar pulse glow bằng CSS @keyframes (không dùng Framer Motion):
     - avatar-glow: box-shadow pulse 3.5s ease-in-out (inner, inset -12px)
     - avatar-glow-outer: box-shadow pulse 3.5s, delay 1.2s (outer, inset -20px)
     → GPU composited, không tốn JS thread
  2. Online dot: thêm class dot-pulse (CSS @keyframes opacity+scale 2s)
  3. Scroll reveal bidirectional:
     - Đổi tất cả viewport={{ once: true }} → viewport={{ once: false }}
     → Elements ẩn khi vuốt lên, hiện từ từ khi vuốt xuống

Files thay đổi:
  - apps/portfolio/src/pages/Home.tsx
  - apps/portfolio/src/index.css (thêm @keyframes avatar-glow, avatar-glow-outer, dot-pulse)

GitHub commit: 1c06217b7a3536e0151c91caa6371c6f0a9ad20c
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 30] PERF — Fix lag triệt để portfolio (session 29 hết quota giữa chừng)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề (session 29 phát hiện, session 30 hoàn thiện):
  1. backdropFilter: blur(16px) trong AnimBorderCard — killer #1 trên mobile
  2. MorphOrb x3 — filter: blur(80px) + Framer Motion animate liên tục
  3. Video CSS filter — brightness+grayscale mỗi frame (GPU tốn)
  4. ~6 Framer Motion animate loops chạy vĩnh viễn:
     - Avatar pulse glow (2 divs)
     - Online dot scale pulse
     - Badge dot opacity blink
     - Tagline spans opacity pulse
     - ChevronDown y bounce
     - LIVE badge dot blink

Đã fix:
  1. AnimBorderCard: xóa backdropFilter hoàn toàn, chỉ giữ background solid
  2. Xóa toàn bộ MorphOrb component + 3 instances khỏi JSX
  3. Video: đổi filter CSS → opacity: 0.38 + tăng overlay rgba(0,0,0,0.65)
  4. Xóa 6 animate loops:
     - 2 pulse glow divs → xóa hẳn
     - Online dot → <span> tĩnh
     - Badge dot → <span> tĩnh
     - Tagline spans → <span> HTML tĩnh
     - ChevronDown → static icon (không bounce)
     - LIVE badge dot → <span> tĩnh
  5. Footer: xóa backdropFilter
  6. Avatar trong giới-thiệu: xóa 2 mini rings (giảm will-change overlap)
  7. ScrollProgress: đổi motion.div → plain <div> với width state
  8. ProjectCard: xóa hover glow AnimatePresence + filter: blur(10px)
  9. Xóa import AnimatePresence, useMotionValue (hero), springX/Y, handleMouseMove hero

Kết quả:
  - Từ ~6 Framer Motion loops liên tục → 0
  - Xóa 3 filter: blur(80px) (MorphOrb)
  - Xóa backdropFilter khỏi mọi AnimBorderCard card
  - CSS rings (ring-cw/ring-ccw) giữ nguyên → GPU composited, không tốn JS

Files thay đổi:
  - apps/portfolio/src/pages/Home.tsx (rewrite)

GitHub commit: d3cf4f6fced3f70a60b9264043c8593b824851b4
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 29] PERF — Tối ưu animation, giảm lag portfolio
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề: Quá nhiều animation đồng thời → lag trên thiết bị yếu

Đã làm:
  1. Xóa FloatingParticles (22 Framer Motion anims liên tục) khỏi Home.tsx
  2. Bio/Skills/Timeline cards: thay AnimBorderCard → plain glass div (không running-border)
  3. AnimBorderCard dùng prop animate={boolean} để pause/resume:
     - Thêm prop `animate` vào AnimBorderCard + AnimBorderItem
     - CSS: .animation-paused::before { animation-play-state: paused }
     - Avatar card, Project cards chỉ animate khi section đang in viewport (useInView)
  4. Navigation: chỉ tab active mới có running-border (inactive = plain motion.a)
  5. ContactCard rewritten: xóa AnimBorderCard, xóa whileHover phức tạp
     → plain glass div + chỉ fade-in entrance (1 lần, không lặp)
  6. ProjectCard: truyền animate={isProjectsInView} vào AnimBorderCard
  7. Xóa import ArrowUpRight không dùng

Kết quả:
  - Từ ~15+ running-border đồng thời → còn 1-3 (chỉ khi in viewport)
  - Xóa 22 particle animations
  - Contact: từ 8 running-border → 0
  - Menu: từ 6 running-border → 1

Files thay đổi:
  - apps/portfolio/src/pages/Home.tsx
  - apps/portfolio/src/components/navigation.tsx
  - apps/portfolio/src/index.css
  - .replit

GitHub commit: (session 29)
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 28] FIX — Running border đúng kiểu (mask-composite)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề session 27 chưa đúng:
  - conic-gradient xoay từ tâm element → thấy gradient rộng ở giữa, không phải "đường chạy theo viền"
  - User muốn: một đường sáng ngắn chạy theo đường viền của element

Fix session 28 — kỹ thuật mask-composite:
  - ::before pseudo-element với padding: 1.5px + conic-gradient (arc ngắn ~13%)
  - mask: content-box + normal box → mask-composite: exclude
  - Kết quả: chỉ thấy 1.5px viền, phần giữa hoàn toàn ẩn
  - Không còn gradient xoay giữa, không DOM element nào di chuyển
  - Hoạt động với mọi border-radius

CSS: .running-border + .running-border::before (thay thế .anim-border/.anim-border-inner)
React: AnimBorderCard/AnimBorderItem dùng single div (không cần inner wrapper)

Files thay đổi:
  - apps/portfolio/src/index.css (thêm .running-border + ::before)
  - apps/portfolio/src/pages/Home.tsx
  - apps/portfolio/src/components/navigation.tsx
  - apps/portfolio/src/pages/Tool.tsx
  - apps/portfolio/src/pages/tools/YtDownloader.tsx

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 27] FIX — AnimBorderCard lag (CSS @property)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - AnimBorderCard dùng framer-motion rotate 360 trên div lớn inset: -100%
  - Mỗi card = 1 div oversized xoay liên tục → nhiều card cùng xoay → lag

Fix:
  - Dùng CSS @property --ab-angle (typed angle, browser animate native)
  - conic-gradient(from var(--ab-angle), ...) thay conic-gradient xoay DOM
  - Không còn DOM element nào xoay → GPU handle hoàn toàn
  - Tương tự: SpinRing (avatar rings) đổi sang @keyframes ring-cw/ring-ccw
    → CSS transform rotate → vẫn GPU composited

CSS thêm vào index.css:
  @property --ab-angle { syntax: '<angle>'; ... }
  @keyframes ab-spin { to { --ab-angle: 360deg } }
  .anim-border { padding: 1px; background: conic-gradient(from var(--ab-angle),...) }
  .anim-border-inner { backdrop-filter: blur(16px) }
  @keyframes ring-cw/ring-ccw
  .ring-cw / .ring-ccw { animation: ring-cw/ccw var(--ring-speed) linear infinite }

Files thay đổi:
  - apps/portfolio/src/index.css (thêm CSS @property animation)
  - apps/portfolio/src/pages/Home.tsx (AnimBorderCard + 5 avatar rings → CSS)
  - apps/portfolio/src/components/navigation.tsx (SpinRing + AnimBorderItem → CSS)
  - apps/portfolio/src/pages/Tool.tsx (AnimBorderCard → CSS)
  - apps/portfolio/src/pages/tools/YtDownloader.tsx (AnimBorderCard → CSS)

Portfolio: RUNNING ✅ (apps/portfolio: web, port 21113)
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 26] Task #6 — Portfolio Heavy Animation Overhaul
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. Avatar Hero — 3 rings xoay bao bọc:
     - Ring ngoài: dashed, counter-rotate chậm (25s)
     - Ring giữa: solid, xoay nhanh (9s) với border gradient
     - Ring trong: xoay rất nhanh (5s), mỏng
     - Pulse glow 2 lớp với độ trễ lệch pha
     - Online dot có animation pulse
     - Parallax mouse-follow dùng useSpring

  2. Năm sinh: 2007 → 2012

  3. AnimBorderCard component (dùng lại toàn bộ):
     - Rotating conic-gradient border (1px padding, inset -100%)
     - Tốc độ tăng khi hover
     - Áp dụng cho: tất cả timeline cards, project cards,
       contact cards, bio cards, skill section, CTA button

  4. Navigation menu avatar:
     - Thay icon "N" bằng ảnh avatar thật của Khang
     - 2 rings xoay bao bọc (dashed + solid)
     - Pulse glow
     - Online dot animated
     - AnimBorderItem áp dụng cho mỗi nav item

  5. Tool.tsx: đổi toàn bộ màu tím (#7c3aed, #a78bfa) → đen/trắng
     - iconColor, iconBg, iconBorder, tagColor → monochrome
     - Background → #050505
     - Tất cả accent violet → rgba(255,255,255,0.x)
     - AnimBorderCard cho search bar và tool cards
     - 3D tilt với useSpring
     - Floating particles, morphing orbs

  6. YtDownloader.tsx: đổi màu tím → đen/trắng
     - Download button: gradient tím → monochrome glass
     - Input focus → white glow thay vì purple
     - Quality buttons: purple active → white active
     - AnimBorderCard cho input, icon header, download button
     - Floating particles, animated orbs

  7. Thêm rất nhiều animation (req #6):
     - FloatingParticles (22 hạt bay)
     - ScrollProgress bar (top of page)
     - MorphOrb (3 orbs biến dạng liên tục)
     - AnimCounter (đếm số: 3+ năm, 10+ projects)
     - Mouse parallax trên hero
     - SectionHeader: character-by-character reveal
     - Skills chips: stagger + spring hover
     - Timeline dots: scale bounce khi xuất hiện
     - Project cards: 3D tilt dùng useSpring
     - Contact cards: whileHover y-4 + scale + icon rotate
     - Nav close button: rotate 90° on hover
     - Orbs in Tool/YtDownloader animate scale+opacity
     - Sparkles icon shake animation
     - Platform badges animate on mount

Files thay đổi:
  - apps/portfolio/src/pages/Home.tsx (viết lại)
  - apps/portfolio/src/components/navigation.tsx (viết lại)
  - apps/portfolio/src/pages/Tool.tsx (viết lại)
  - apps/portfolio/src/pages/tools/YtDownloader.tsx (viết lại)

GitHub commit: 0f9c00d50a9af29f2acfd80c39927faa19771d7f
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 25] Task #2 — Portfolio Full Page Redesign
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. Video background đổi sang `position: fixed` (z-index: -2) → phủ toàn trang khi scroll
     - Thêm overlay đen cố định cùng layer (rgba(0,0,0,0.55))
     - filter: brightness(0.38) grayscale(0.2) để phù hợp thiết kế grayscale

  2. Đổi font toàn trang: Cinzel/Inter → Plus Jakarta Sans (weight 300-800)
     - index.html: cập nhật Google Fonts link
     - Tất cả fontFamily đổi sang Plus Jakarta Sans

  3. Hero section gọn lại:
     - Chỉ còn: avatar nhỏ (96px), badge, tên (font 800), tagline 2 dòng, nút scroll
     - Xóa contact grid khỏi hero

  4. Design system grayscale hoàn toàn:
     - Tất cả cards/elements: rgba(255,255,255,0.05) bg, rgba(255,255,255,0.12) border
     - borderRadius: 20px, backdropFilter: blur(16px)
     - Không còn màu accent tím/xanh/hồng — chỉ trắng/xám

  5. Section A — Giới thiệu (#gioi-thieu):
     - Avatar lớn (140px) + thông tin card bên trái
     - 2 đoạn bio text, danh sách 15 kỹ năng dạng chips

  6. Section B — Lịch sử (#lich-su):
     - Timeline dọc 7 mốc (2021 → 2026)
     - Đường thẳng dọc + dot trắng + card glass cho mỗi mốc
     - Nội dung dài, đầy đủ ý cho từng mốc

  7. Section C — Projects (#du-an):
     - 3 cards: NexoraGarden, NexoraAI, NexoraTool
     - Tags, mô tả đầy đủ, link GitHub
     - NexoraGarden: badge LIVE + emerald dot

  8. Section D — Liên hệ (#lien-he):
     - Grid 2x4 (mobile) / 4x2 (desktop), 8 kênh đầy đủ
     - GitHub, Email, Facebook, Telegram, SĐT, Zalo, Instagram, TikTok
     - Grayscale icons, hover effect nhẹ

  9. Navigation.tsx viết lại:
     - Toàn bộ đổi sang màu đen: nút hamburger nền đen, sidebar #0a0a0a
     - Thêm links: Giới thiệu (#gioi-thieu), Lịch sử (#lich-su), Liên hệ (#lien-he)
     - Active state: trắng thay vì tím
     - Accent indicator: white/60 thay tím

Files thay đổi:
  - apps/portfolio/src/pages/Home.tsx (viết lại hoàn toàn)
  - apps/portfolio/src/components/navigation.tsx (viết lại hoàn toàn)
  - apps/portfolio/index.html (đổi font sang Plus Jakarta Sans)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 24] Task #1 — Portfolio Hero Page Redesign (video bg)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. Xóa toàn bộ giao diện cũ (GlassScene + glass morphism)
  2. Viết lại Home.tsx hoàn toàn:
     - Video background: Cloudinary dds3_1_rqhg7x.mp4 (autoPlay loop muted playsInline)
     - Dark overlay gradient phủ lên video
     - Avatar + badge + tên "Phan Trọng Khang" font Cinzel cinematic
     - Subtitle + bio ngắn tiếng Việt
     - 8 contact cards grid responsive:
       GitHub, Email, Facebook, Telegram, SĐT, Zalo, Instagram, TikTok
     - Projects section (NexoraGarden + Arcane Tools)
     - Footer đơn giản
  3. Updated index.html: thêm Google Fonts (Cinzel + Inter)
  4. react-icons dùng cho FaFacebook, FaTelegram, FaInstagram, FaTiktok, SiZalo
  5. Mobile-optimized: touch targets, font clamp, grid 2-3-4 cols, playsInline
  6. Push GitHub: commit 04e7822b (Tree API, 1 commit)
  7. Gửi Telegram thông báo

Files thay đổi:
  - apps/portfolio/src/pages/Home.tsx (viết lại hoàn toàn)
  - apps/portfolio/index.html (thêm Cinzel + Inter fonts)
  - apps/portfolio/src/components/GlassScene.tsx (giữ nguyên file, không import nữa)
  - apps/portfolio/src/components/ThreeScene.tsx (giữ nguyên file, không import nữa)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 23b] FIX — Portfolio index.html dư thừa
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - apps/portfolio/index.html là file HTML tĩnh 742 dòng (từ session 21b)
  - Vite đang serve file HTML cũ này thay vì React app
  - Kết quả: nhìn thấy trang cũ, không thấy giao diện Glass Morphism mới

Đã fix:
  - Thay apps/portfolio/index.html bằng Vite entry point đúng (13 dòng)
    → chỉ có <div id="root"> + <script src="/src/main.tsx">
  - React app (GlassScene.tsx + Home.tsx glass morphism) load bình thường
  - Push GitHub: commit 1 lần duy nhất (Tree API)
  - Restart "apps/portfolio: web" → RUNNING ✅

Files thay đổi:
  - apps/portfolio/index.html (thay hoàn toàn — file cũ 742 dòng → Vite entry 13 dòng)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 23] MIGRATION — Replit environment mới (lần 7)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install → 730 packages OK
  2. npm install -g node-gyp
  3. node-gyp configure (trong better-sqlite3 dir) → OK
     better_sqlite3.node đã tồn tại từ pnpm install scripts → OK
  4. Restart "apps/portfolio: web" (port 21113) → RUNNING ✅
  5. Restart "packages/api-server: API Server" (port 8080) → RUNNING ✅
  6. Gửi Telegram thông báo hoàn tất ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - SQLite DB initialized ✅
  - ffmpeg native + yt-dlp: sẵn sàng ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt

WARN (không phải lỗi):
  - NexoraGarden/Portfolio dist not found → bình thường dev mode
  - attached_assets/* workflows: FAILED → bình thường (chỉ là file tham khảo cũ)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 22b] PORTFOLIO HOME REDESIGN — Glass Morphism
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. Xóa toàn bộ giao diện Home cũ
  2. Tạo Three.js scene mới (GlassScene.tsx):
     - Aurora ribbons (5 dải sóng màu)
     - Floating glowing orbs (5 quả cầu phát sáng)
     - Neural particles (280 hạt nổi trôi)
     - Shooting stars
     - Camera parallax theo chuột
  3. Avatar mới: IMG_20260410_113130_1775795500020.jpg
     → copy → apps/portfolio/src/assets/avatar_new.jpg
  4. Animation bao bọc avatar:
     - Ring gradient xoay (12s/vòng)
     - Ring ngược chiều (20s/vòng, đứt đoạn)
     - 10 chấm bi xanh/tím xoay quanh (28s)
     - 6 chấm bi xanh lá ngược chiều (18s)
     - Pulse rings nhấp nháy (2.5s chu kỳ)
     - Glow halo radial gradient
  5. Glass morphism toàn trang: bo tròn, backdrop-blur, border white/10
  6. Liên hệ đầy đủ 7 kênh: GitHub, Email, Facebook, Telegram,
     SĐT/Zalo, Instagram, TikTok
  7. Task #1 "Portfolio Home Redesign" đã tạo trong project tasks

Files thay đổi:
  - apps/portfolio/src/pages/Home.tsx (viết lại hoàn toàn)
  - apps/portfolio/src/components/GlassScene.tsx (tạo mới)
  - apps/portfolio/src/assets/avatar_new.jpg (ảnh mới)

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[10/04/2026 session 22] MIGRATION — Replit environment mới (lần 6)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install → 730 packages OK
  2. npm install -g node-gyp
  3. Rebuild better-sqlite3 native binary:
     - node-gyp configure (trong better-sqlite3 dir)
     - node-gyp build (chạy background ~90s)
     → build/Release/better_sqlite3.node ✅
  4. Restart "Start application" (Portfolio) → RUNNING ✅
  5. Tạo lại workflow "API Server":
     - Lệnh: PORT=8080 pnpm --filter @workspace/api-server run dev
     → RUNNING ✅
  6. Gửi Telegram thông báo hoàn tất ✅

Kết quả:
  - Portfolio: RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - SQLite DB initialized ✅

Lưu ý mỗi khi env reset (lần 6 rồi, cần làm quen):
  1. pnpm install
  2. npm install -g node-gyp
  3. cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     node-gyp configure && node-gyp build (chạy background ~90s)
  4. Restart "Start application" + tạo lại "API Server" workflow

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[09/04/2026 session 21] MIGRATION — Replit environment mới (lần 5)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install → 730 packages OK
  2. npm install -g node-gyp
  3. Rebuild better-sqlite3 native binary:
     - cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     - node-gyp configure && node-gyp build
     → build/Release/better_sqlite3.node ✅
  4. Restart "Start application" (Portfolio port 21113) → RUNNING ✅
  5. Tạo workflow "API Server":
     - Lệnh: PORT=8080 pnpm --filter @workspace/api-server run dev
     → RUNNING ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - WebSocket /ws và /ws-browser: sẵn sàng ✅
  - SQLite DB initialized ✅
  - ffmpeg native: /nix/store/.../bin/ffmpeg ✅
  - yt-dlp: ready ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt (set trong Secrets nếu cần)

Lưu ý WARN (không phải lỗi):
  - "NexoraGarden dist not found" + "Portfolio dist not found":
    → Chỉ ảnh hưởng khi api-server serve static (production mode)
    → Dev mode: frontend dùng Vite dev server riêng → không cần build dist

Lưu ý mỗi khi env reset:
  1. pnpm install
  2. npm install -g node-gyp
  3. cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     node-gyp configure && node-gyp build
  4. Restart "Start application" + "API Server"

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[09/04/2026 session 21b] PORTFOLIO REBUILD — Single HTML file + Three.js
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa toàn bộ React structure, viết lại apps/portfolio/index.html
thành single HTML file (HTML + CSS + JS inline, no framework).

Tính năng:
  - Three.js từ CDN (r0.160.0) — full galaxy background
  - Galaxy band 16,000 particles (spiral arms, 3 nhánh)
  - Deep stars 3,500 điểm (spectral colors)
  - Nebula 4 cụm (additive blending)
  - Crystals x6 (Icosahedron/Octahedron/Dodecahedron wireframe + glow)
  - Connection lines giữa crystals
  - Data stream 180 particles chạy dọc connections
  - Aurora ribbons x5 (wave animation realtime)
  - Shooting stars x8 (ngẫu nhiên)
  - Mouse parallax camera
  - Custom cursor với ring effect

UI:
  - Hamburger menu góc trên trái → sidebar (Nav: Trang chủ/Dự án/Liên hệ)
  - Hero: "Phan Trọng Khang", "AI Architect & Developer", bio tiếng Việt
  - Scroll reveal animations
  - Projects grid: NexoraGarden (đang PT) + 3 placeholder "Sắp ra mắt"
  - Contact grid: GitHub, Email, Facebook, Telegram, SĐT, Zalo, Instagram, TikTok
  - Footer

Liên hệ đã thêm:
  - GitHub: khang26042012
  - Email: trongkhabgphan@gmail.com
  - Facebook: https://www.facebook.com/share/1CAZqbwCgB/
  - Telegram: +84352234521
  - SĐT: 0352234521
  - Zalo: 0352234521
  - Instagram: khang.trong.809039
  - TikTok: @phantrongkhangg

Fonts: Orbitron (headings) + Inter (body) từ Google Fonts

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[09/04/2026 session 20] MIGRATION — Replit environment mới (lần 4)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install → 730 packages OK
  2. Fix portfolio dev script: dùng "node node_modules/vite/bin/vite.js"
     thay vì "vite" trực tiếp (pnpm không tạo .bin symlinks trong env này)
  3. Fix .npmrc: thêm better-sqlite3, esbuild, msw, unrs-resolver, @swc/core vào allow-build
  4. Rebuild better-sqlite3 native binary:
     - npm install -g node-gyp
     - cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     - node-gyp rebuild --release → better_sqlite3.node ✅
  5. Restart "Start application" (Portfolio port 21113) → RUNNING ✅
  6. Restart "packages/api-server: API Server" (port 8080) → RUNNING ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - WebSocket /ws và /ws-browser: sẵn sàng ✅
  - SQLite DB initialized ✅
  - ffmpeg native: /nix/store/.../bin/ffmpeg ✅
  - yt-dlp: ready ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt (set trong Secrets nếu cần)

Lưu ý mỗi khi env reset:
  1. pnpm install
  2. npm install -g node-gyp
  3. cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     node-gyp rebuild --release
  4. Restart "Start application" + "packages/api-server: API Server"

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[09/04/2026 session 19] MIGRATE — Bỏ Back4App, chuyển sang Railway
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Lý do: Chuyển sang Railway — $5 credit/tháng miễn phí, không cần thẻ,
GitHub auto-deploy, Docker, PORT inject tự động, không sleep.

Đã làm:
  - railway.json tạo mới (build: DOCKERFILE, deploy: startCommand + healthcheck)
  - DEPLOY.md viết lại hoàn toàn cho Railway (step-by-step)
  - Dockerfile cập nhật comment (PORT từ Railway inject, không cố định)
  - replit.md cập nhật platform → Railway

NEXT STEPS cho Khang:
  1. railway.app → Sign Up with GitHub (khang26042012)
  2. New Project → Deploy from GitHub → repo Nexora, branch main
  3. Variables: set TELEGRAM_TOKEN, TELEGRAM_CHAT_ID, GEMINI_API_KEY,
     TELEGRAM_WEBHOOK_URL, TELEGRAM_WEBHOOK_SECRET, WEATHER_API_KEY
     (PORT không cần set — Railway inject tự động)
  4. Volumes: mount /app/packages/api-server/data
  5. Đợi build lần đầu (~5-10 phút)
  6. Lấy domain railway.app → Cloudflare DNS + Custom Domain

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[09/04/2026 session 18] MIGRATE — Bỏ Northflank, chuyển sang Back4App Containers (đã hủy)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Lý do: Northflank yêu cầu thêm thẻ thanh toán (dù free).
Back4App Containers: hoàn toàn miễn phí, không cần thẻ,
GitHub auto-deploy, Docker, 0.25vCPU + 256MB RAM.

Đã làm:
  - DEPLOY.md viết lại cho Back4App (step-by-step)
  - replit.md cập nhật platform → Back4App Containers
  - Dockerfile + .dockerignore giữ nguyên (Back4App cũng dùng Docker)
  - Không cần thay đổi code gì thêm

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[09/04/2026 session 17] MIGRATE — Bỏ Wispbyte, chuyển sang Northflank (đã hủy)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Lý do: Wispbyte dùng Pterodactyl panel — startup command phức tạp,
không đủ linh hoạt cho pnpm monorepo. Northflank: Docker container,
GitHub auto-deploy, free tier 2 services + 500 build minutes/tháng.

Đã làm:
  1. Tạo Dockerfile (root repo):
     - Base: node:20-slim + python3/make/g++ cho better-sqlite3
     - pnpm install --frozen-lockfile (better-sqlite3 tự build)
     - Build: nexora-garden + portfolio + api-server
     - CMD: node packages/api-server/dist/index.mjs
     - Layer cache tối ưu: copy package.json trước, install, rồi copy source

  2. Tạo .dockerignore:
     - Bỏ qua node_modules, .git, attached_assets, mockup-sandbox
     - Bỏ qua dist/ (build lại trong Docker)

  3. DEPLOY.md viết lại cho Northflank:
     - Bước tạo account + kết nối GitHub
     - Tạo Deployment Service (Dockerfile, port 8080, auto-deploy)
     - Set Environment Variables
     - Mount Persistent Volume tại /app/packages/api-server/data (SQLite)
     - DNS Cloudflare → Northflank domain
     - Cloudflare Worker ping

  4. replit.md cập nhật platform → Northflank

SQLite DB path: /app/packages/api-server/data/nexora.db
Persistent volume cần mount tại: /app/packages/api-server/data

NEXT STEPS cho Khang:
  1. northflank.com → Sign Up → thêm payment method
  2. Tạo Project → Add Service → Deployment Service → GitHub
  3. Chọn repo Nexora, branch main, Dockerfile
  4. Set ENV vars + mount volume /app/packages/api-server/data
  5. Deploy → cập nhật Cloudflare DNS

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[09/04/2026 session 16] MIGRATE — Bỏ Zeabur, chuyển sang Wispbyte (đã hủy)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Lý do: Zeabur yêu cầu mua dedicated server ($2+/tháng), không còn free tier.
Wispbyte: free, 24/7, container-based, hỗ trợ Node.js.

Đã làm:
  1. Tạo startup.sh (root repo) — smart startup script:
     - Cài pnpm nếu chưa có
     - pnpm install --ignore-scripts
     - Build better-sqlite3 native binary (chỉ lần đầu)
     - Build all packages (chỉ nếu dist/ chưa tồn tại)
     - Start: node packages/api-server/dist/index.mjs
     - Startup command trong Wispbyte: bash startup.sh

  2. DEPLOY.md viết lại cho Wispbyte:
     - Bước upload source qua File Manager / SFTP
     - Set Startup Command, Environment Variables
     - Hướng dẫn DNS Cloudflare → wispbyte subdomain
     - Cloudflare Worker ping (giữ container alive)
     - Quy trình cập nhật code thủ công

  3. replit.md cập nhật platform → Wispbyte
  4. zbpack.json giữ nguyên (không ảnh hưởng Wispbyte)

Push lên GitHub: commit 4bf6c030 (session 15) + commit mới (session 16)

NEXT STEPS cho Khang:
  1. Đăng ký wispbyte.com → Create Server (Generic Node.js)
  2. Upload source code (trừ node_modules/, .git/, attached_assets/)
  3. Startup Command: bash startup.sh
  4. Set Environment Variables
  5. Start → đợi build lần đầu ~5 phút
  6. Lấy subdomain → cập nhật Cloudflare DNS

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[08/04/2026 session 15] MIGRATE — Bỏ Render, chuyển sang Zeabur (đã hủy)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Lý do: Render free tier lag nặng, nhiều người deploy → timeout liên tục.

Đã làm:
  1. Tạo zbpack.json (root repo) — cấu hình Zeabur:
     - install_command: pnpm install
     - build_command: build nexora-garden + portfolio + api-server
     - start_command: node packages/api-server/dist/index.mjs
     - cache_dependencies: false (tránh lỗi pnpm workspace)

  2. Cập nhật package.json (root):
     - Thêm "packageManager": "pnpm@10.26.1" → Zeabur dùng pnpm
     - Thêm "engines": { "node": ">=20" } → pin Node version

  3. Viết lại DEPLOY.md hoàn toàn:
     - Hướng dẫn deploy Zeabur step-by-step
     - Danh sách env vars cần set (thêm YOUTUBE_COOKIES, WEATHER_API_KEY)
     - Cloudflare DNS: đổi CNAME về Zeabur URL
     - Giữ lại Cloudflare Worker ping, ESP32 firmware, YtDownloader notes

  4. Cập nhật replit.md:
     - Thay "Render.com" → "Zeabur"
     - Thêm YOUTUBE_COOKIES vào env vars list

Hướng dẫn Khang deploy Zeabur:
  1. zeabur.com → New Project → GitHub → khang26042012/Nexora
  2. 1 service duy nhất (API Server) — Zeabur detect zbpack.json tự động
  3. Variables: TELEGRAM_TOKEN, TELEGRAM_CHAT_ID, GEMINI_API_KEY, TELEGRAM_WEBHOOK_URL,
     TELEGRAM_WEBHOOK_SECRET, WEATHER_API_KEY, NODE_ENV=production, YOUTUBE_COOKIES (tùy chọn)
  4. Cloudflare DNS: CNAME nexorax.cloud → <zeabur-service>.zeabur.app (Proxied)
  5. Xóa service Render sau khi xác nhận Zeabur hoạt động

ESP32 cần nạp firmware: KHÔNG (server vẫn tại nexorax.cloud)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[08/04/2026 session 14] THREE.JS REDESIGN V2 — Galaxy + Aurora + AI Crystals
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa toàn bộ scene cũ (rocket, astronaut, moon, earth), viết lại hoàn toàn ThreeScene.tsx:

Concept: Galaxy + Aurora Borealis + AI Crystal Network

Các thành phần:
  - GalaxyBand: 18,000 particles tạo dải Ngân Hà thực (warm core → cool edges, chậm quay)
  - DeepStars: 2,500 sao nền tĩnh (màu xanh/vàng theo spectral type)
  - BrightStars: 80 sao sáng nhấp nháy foreground
  - Aurora: 5 ribbon sóng (xanh lá, xanh dương, tím) ở phía dưới — waving animation realtime
  - ShootingStars: 6 sao băng ngẫu nhiên, spawn định kỳ, bay chéo
  - Crystal (×6): Icosahedron/Octahedron/Dodecahedron wireframe + glow, màu gradient (tím/cyan/hồng/xanh lá)
  - ConnectionLines: đường nối tĩnh giữa các crystal gần nhau (neural net look)
  - DataStreams: 120 particles chạy dọc các đường nối (data flow animation)
  - Nebula: 3 cụm dust mờ (additive blending)
  - SceneCamera: mouse parallax mượt

Kỹ thuật:
  - Tất cả materials dùng AdditiveBlending → glow effect tự nhiên
  - Aurora dùng PlaneGeometry realtime vertex deformation (sin wave)
  - ShootingStars dùng BufferAttribute update mỗi frame
  - Performance: dpr cap 1.8 desktop / 1.5 mobile, antialias off

Ghi chú:
  - WebGL error trong Replit preview là bình thường (headless env)
  - Trên browser thật: hoạt động đầy đủ 60fps
  - ThreeSections.tsx (orbital rings per-section) giữ nguyên
  - ESP32: KHÔNG cần nạp lại firmware

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[08/04/2026 session 13] THREE.JS PORTFOLIO REDESIGN — Space background
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Redesign hoàn toàn background portfolio thành full-page Three.js space scene:

ThreeScene.tsx — viết lại hoàn toàn:
  - Stars: 2000 particle stars (gradient màu xanh/tím/trắng), 60 accent twinkling stars
  - Moon: sphere bên phải với procedural crater texture (canvas-based, ~35 craters)
  - Earth: large sphere ở bottom edge với atmosphere + corona glow layers
  - Rocket: geometric mesh (body/nose/3fins/porthole/stripes), floating animation
  - Astronaut: full-body figure (helmet+visor+torso+arms+legs+boots), bobbing drift
  - ExhaustParticles: particle trail behind rocket
  - Nebula: subtle fog particle clusters (4 defs, additive blending)
  - Lights: ambientLight + directionalLight (sunlight) + pointLight (earth bounce)
  - SceneCamera: mouse parallax + gentle drift animation
  - Fallback: CSS dark background nếu WebGL không khả dụng

Home.tsx:
  - ThreeScene → position:fixed, inset:0, z-index:0, pointer-events:none (full-page)
  - Xóa ThreeScene cũ ra khỏi hero section
  - Background main div: solid #000510 thay vì gradient

Ghi chú:
  - WebGL error trong Replit preview là bình thường (headless env, không hỗ trợ WebGL)
  - Trên browser thật: hoạt động đầy đủ 60fps
  - ThreeSections.tsx (orbital rings per-section) giữ nguyên, overlay trên background
  - ESP32: KHÔNG cần nạp lại firmware

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 12] TIKTOK DOWNLOAD — tích hợp tikwm.com API
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề: yt-dlp bị TikTok block IP datacenter → dùng tikwm.com free API

Đã làm (packages/api-server/src/routes/youtube.ts):
  1. Thêm hàm isTikTok(url) — detect tiktok.com / vm.tiktok.com
  2. Thêm hàm fetchTikTokData(url) — POST tikwm.com/api/, trả data.play/hdplay/cover/title
  3. Thêm hàm streamRemoteUrl() — stream CDN URL trực tiếp về client
  4. Sửa GET /api/yt/info: nếu TikTok → gọi tikwm, trả {platform:"TikTok", qualities:["best","360p"], ...}
  5. Sửa GET /api/yt/download: nếu TikTok → stream hdplay (best) hoặc play (360p)

Kết quả test:
  - /api/yt/info?url=tiktok → platform:TikTok, channel:..., thumbnail:ok ✅
  - /api/yt/download?quality=best → HTTP 200, video/mp4, 3.9MB ✅
  
Ghi chú:
  - tikwm.com chỉ hoạt động với video còn tồn tại (không cache video quá cũ)
  - tikwm chấp nhận: www.tiktok.com/@user/video/ID, vm.tiktok.com/short/
  - quality "best" → hdplay (HD), "360p" → play (standard, no watermark)
  - ESP32: KHÔNG cần nạp lại firmware

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 11] SETUP — Migration Replit environment mới (lần 3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → 730 packages (done in 36.7s)
  2. npm install -g node-gyp --prefix=/tmp/gyp-install
  3. Rebuild better-sqlite3 native binary:
     - node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     - node-gyp configure && build → better_sqlite3.node ✅
  4. Restart workflow "Start application" (Portfolio port 21113) → RUNNING ✅
  5. Tạo workflow "API Server":
     - Lệnh: PORT=8080 pnpm --filter @workspace/api-server run dev
     → RUNNING ✅ (tên mới: "API Server" thay vì "packages/api-server: API Server")

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - WebSocket /ws và /ws-browser: sẵn sàng ✅
  - SQLite DB initialized ✅
  - ffmpeg native detected: /nix/store/.../bin/ffmpeg ✅
  - yt-dlp binary: ready ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt (cần set trong Secrets)

Lưu ý mỗi khi env reset:
  1. pnpm install --ignore-scripts
  2. npm install -g node-gyp --prefix=/tmp/gyp-install
  3. cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     /tmp/gyp-install/bin/node-gyp configure && /tmp/gyp-install/bin/node-gyp build
  4. Restart "Start application" + "API Server"

Tên workflow API Server hiện tại: "API Server" (KHÔNG phải "packages/api-server: API Server")

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 11] FIX — YOUTUBE_COOKIES + node JS runtime cho yt-dlp
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Khang gửi file cookies YouTube (tài khoản VN). Đã làm:
  1. Copy cookies vào /tmp/yt-cookies.txt
  2. Set Secret YOUTUBE_COOKIES = nội dung file (Replit Secrets)
  3. Sửa setupCookies(): fallback check fs.existsSync(/tmp/yt-cookies.txt)
  4. Thêm findNodeBin() + --js-runtimes node:PATH vào baseArgs()
     → yt-dlp 2026 cần JS runtime để decipher YouTube (deprecated warning fix)
  5. Restart API Server → log: cookies:true ✅

Kết quả test trên Replit (IP datacenter Mỹ):
  - Cookie setup: ✅ cookies:true trong log
  - Video VN geo-blocked: vẫn "Video unavailable"
    → Nguyên nhân: YouTube block IP datacenter Mỹ ở tầng IP, không phải account
    → Cookies chỉ fix age-restricted/account-only, KHÔNG fix IP geo-block cứng
  - Cần test trên Render production (IP khác) → có thể bypass được

Để deploy Render: push GitHub + set YOUTUBE_COOKIES trong Render Environment Variables

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[08/04/2026 session 11] FEAT — YtDownloader: hiển thị quality có sẵn theo video
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề: UI có 4 nút quality cố định, Khang không biết cái nào tải được cho video đó.

Fix:
  1. Server /api/yt/info: phân tích formats, tính maxHeight, trả về:
     - availableQualities: ["best","720p","480p","360p"] (chỉ những cái video có)
     - maxQuality: "720p" (cao nhất có sẵn)
  2. Frontend YtDownloader.tsx:
     - Thêm nút 1080p vào danh sách
     - Nút không có trong availableQualities → mờ (disabled), cursor not-allowed
     - Hiển thị dấu "—" trên nút không có
     - Chú thích "Nút mờ = video không có chất lượng đó"
     - Hiển thị "Cao nhất: 720p" bên cạnh tiêu đề chọn quality
     - Tự động chọn quality tốt nhất khi load xong video info

GitHub commit: adde380 (2 files)
ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 10] FIX — YtDownloader v14: Geo-block diagnosis + hướng dẫn cookies
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Nguyên nhân lỗi video Việt Nam được xác nhận:
  - YouTube dùng IP-based geo-restriction (không phải header-based)
  - `--geo-bypass-country VN` chỉ set X-Forwarded-For header → YouTube bỏ qua
  - Server đặt tại Mỹ (Replit/Render) → YouTube trả "Video unavailable" cho video VN
  - Kiểm tra: Rick Astley ✅, Sơn Tùng / Câu Cá Vạn Cần ❌ từ server Mỹ
  - Test tất cả clients (tv_embedded, android, ios, mweb) → đều fail như nhau

Giải pháp duy nhất (không có VPN/proxy VN):
  YOUTUBE_COOKIES từ tài khoản Google VN của Khang
  → YouTube phục vụ theo region của account, không theo server IP

Fix đã làm (v14 → cùng version, chỉ cải thiện UX):
  1. Backend: error message "GEO_BLOCKED: ..." thay vì generic "Video không tải được..."
     - Cả /info và /download endpoints
  2. Frontend YtDownloader.tsx: detect error.includes("GEO_BLOCKED")
     → Hiện hướng dẫn step-by-step cài YOUTUBE_COOKIES:
       1. Cài Chrome extension "Get cookies.txt LOCALLY"
       2. Mở youtube.com đã đăng nhập tài khoản Google VN
       3. Export cookies → copy nội dung file
       4. Render.com → Settings → Environment → thêm YOUTUBE_COOKIES
  3. Backend đã có cookies handler từ trước:
     - Đọc env YOUTUBE_COOKIES → ghi /tmp/yt-cookies.txt
     - Truyền --cookies /tmp/yt-cookies.txt cho yt-dlp

Trạng thái:
  - Non-VN videos (Rick Astley, etc.): tải tốt ✅
  - Video VN: cần YOUTUBE_COOKIES, UI hiện hướng dẫn rõ ràng ✅
  - Replit: set Secret YOUTUBE_COOKIES khi có cookies
  - Render (production): set Environment Variable YOUTUBE_COOKIES

ESP32 cần nạp firmware: KHÔNG

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 9] FIX — YtDownloader v13: format selector + tv_embedded first
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts (v13)

Vấn đề từ ảnh chụp:
  - "Requested format is not available" khi download
  - Info (/info) OK nhưng download fail
  - Nguyên nhân: format selector dùng [ext=mp4]+[ext=m4a] quá strict
    → ios/tv_embedded client có format khác codec → không match

Fix đã làm:
  1. Đổi format selector sang yt-dlp shorthand "bv*+ba/b":
     - bv* = best video (any codec) + ba = best audio / b = fallback
     - Tương thích với MỌI client (ios, tv_embedded, android)
     - Thêm --format-sort ext:mp4:m4a để ưu tiên mp4 khi có thể
  2. Đổi strategy order: tv_embedded đầu tiên (KHÔNG cần PO token)
     - tv_embedded → android → ios → mweb
     - Thêm tv_embedded + player_skip=webpage,configs làm fallback
  3. Fix bug: bv* trong block comment gây lỗi build esbuild (dùng // comment)

Tests PASSED:
  - /api/yt/info Rick Astley → title ✅
  - /api/yt/download 720p → HTTP 200 | 21MB ✅
  - /api/yt/download best (4K) → HTTP 200 | 243MB ✅

Lưu ý: video E_IQMXRMV3Y (Câu Cá Vạn Cần) bị geo-restrict
  → chỉ xem được từ Vietnam IP, server US (Replit/Render) không tải được
  → đây là giới hạn của nền tảng, không phải bug code

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 9] FIX — YtDownloader v12: PO Token fix cho YouTube 2026
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts (v12)

Vấn đề gốc:
  - YouTube 2026 bắt buộc PO Token (Proof of Origin) cho web client
  - Datacenter IP (Replit/Render) không tạo được PO Token hợp lệ
  - Strategy cũ: thử web client đầu tiên → luôn fail với PO token error
  - agent cũ hết quota khi đang research

Fix đã làm:
  1. Đổi YT_CLIENT_STRATEGIES — bỏ web client (cần PO token), ưu tiên:
     - Strategy 1: multi-client ios,tv_embedded,android trong 1 lệnh (nhanh nhất)
     - Strategy 2: ios đơn (reliable nhất, không cần PO token)
     - Strategy 3: tv_embedded (không cần PO token, không cần đăng nhập)
     - Strategy 4: android (bypass datacenter IP check)
     - Strategy 5: mweb,web_embedded (fallback cuối)
  2. Thêm PO token errors vào isRetryableErr():
     - po_token, PO Token, proof of origin, nsig, Precondition, 403

End-to-end tests PASSED:
  - /api/yt/info Rick Astley → title ✅ thumbnail ✅ ffmpegAvailable:true ✅
  - /api/yt/download 360p → HTTP 200 | 11.8MB | video/mp4 ✅

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 9] SETUP — Khởi động lại môi trường Replit (migration)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → 730 packages (reused từ cache, done in 26.3s)
  2. npm install -g node-gyp --prefix=/tmp/gyp-install
  3. Rebuild better-sqlite3 native binary:
     - cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     - /tmp/gyp-install/bin/node-gyp configure && build
     → better_sqlite3.node ✅
  4. Restart workflow "Start application" (Portfolio port 21113) → RUNNING ✅
  5. Tạo + start workflow "packages/api-server: API Server":
     - Lệnh: PORT=8080 pnpm --filter @workspace/api-server run dev
     → RUNNING ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - WebSocket /ws và /ws-browser: sẵn sàng ✅
  - SQLite DB initialized ✅
  - ffmpeg native detected: /nix/store/.../bin/ffmpeg ✅
  - yt-dlp binary: ready ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt (cần set trong Secrets)

Lưu ý mỗi khi env reset:
  1. pnpm install --ignore-scripts
  2. npm install -g node-gyp --prefix=/tmp/gyp-install
  3. cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     /tmp/gyp-install/bin/node-gyp configure && /tmp/gyp-install/bin/node-gyp build

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 8] FIX — YtDownloader v11: code review fixes, Task #1 HOÀN THÀNH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts (v11)
File: apps/portfolio/src/pages/tools/YtDownloader.tsx (qualityNote UI)

Các vấn đề từ code review đã fix:

  1. Thêm `web_creator` và `mediaconnect` vào YT_CLIENT_STRATEGIES
     - Tổng 7 strategies: [], android, ios, tv_embedded, web_creator, mediaconnect, mweb
     - mediaconnect test: 31 formats returned ✅

  2. Thêm hasTar() check trước khi extract ffmpeg
     - Nếu `tar` không có → log warning + return sớm
     - Không còn crash lúc extract

  3. Thêm qualityNote vào /api/yt/info response
     - Khi _ffmpegReady = null: qualityNote = "Chất lượng giới hạn ở 360p/720p..."
     - Frontend YtDownloader.tsx hiển thị warning amber khi có qualityNote

  4. downloadWithRetry() — retry/backoff cho yt-dlp binary download
     - 3 lần thử: ngay lập tức → 5s → 15s
     - Log rõ ràng mỗi lần retry

Non-YouTube platform test:
  - Streamable.com: HTTP 200 | 3.0MB | video/mp4 ✅
  - TikTok/Instagram/Twitter: blocked by datacenter IP (expected, out of scope)

End-to-end tests PASSED:
  - YouTube 360p: HTTP 200 | 9.17MB | video/mp4 ✅
  - Streamable (non-YT): HTTP 200 | 3.04MB | video/mp4 ✅
  - /api/yt/info qualityNote field: null when ffmpeg available ✅

Task #1 (Fix tool tải video đa nền tảng) — HOÀN THÀNH


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 7] FIX — YtDownloader v10: native ffmpeg, multi-platform, binary validation
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts
GitHub commit: 6f03f0258fcf32b38b1a0b74073b14a131affc35

Vấn đề gốc rễ:
  - _ffmpegReady luôn null vì code chỉ tìm /tmp/yt-ffmpeg
  - Replit có ffmpeg native (v6.1.2) trong PATH nhưng không được detect
  - → getFormatSelector() luôn dùng combined formats (max 360p/720p itag 22)
  - Multi-platform (TikTok, Twitter) bị loop qua 4 YouTube player_client strategies không cần thiết
  - isBinUsable() chưa có → dùng binary hỏng không biết

Fix đã làm:
  1. findNativeFfmpeg() — tìm ffmpeg từ PATH (which ffmpeg) + common paths
     - Replit: /nix/store/.../bin/ffmpeg → set _ffmpegReady ngay, KHÔNG download 40MB
     - Render: không có native → kickFfmpegDownload() như cũ (fallback)
  2. initFfmpeg() thay kickFfmpegDownload() — gọi lúc startup, ưu tiên native
  3. isBinUsable() — chạy yt-dlp --version để validate binary trước khi dùng
     - Tránh dùng binary sai arch hoặc corrupt
  4. isYouTube(url) — tách platform detection
     - YouTube → YT_CLIENT_STRATEGIES (5 strategies: web, android, tv_embedded, ios, mweb)
     - Non-YouTube (TikTok, Twitter, etc.) → NON_YT_STRATEGIES (1 attempt)
  5. Thêm ios client vào YT_CLIENT_STRATEGIES
  6. Download timeout: 300_000ms (5 phút) cho file lớn
  7. Cleanup thêm .tmp files
  8. /api/yt/info response thêm trường ffmpegAvailable

Kết quả test trực tiếp trên Replit:
  - /api/yt/info Rick Astley → title ✅ thumbnail ✅ ffmpegAvailable:true ✅
  - /api/yt/download 360p → HTTP 200, 8.8MB, video/mp4 ✅
  - /api/yt/download 720p (ffmpeg merge) → HTTP 200, 21MB, video/mp4 ✅
  - /api/yt/download best (4K) → HTTP 200, 233MB, video/mp4 ✅

Ghi chú môi trường:
  - Replit: ffmpeg native → high quality ngay lập tức, không cần download
  - Render: không có ffmpeg native → download 40MB static binary (background, 2-3 phút)
    Trong thời gian chờ: combined formats (360p/720p itag 22/18) vẫn hoạt động
  - TikTok/Instagram: bị block datacenter IP (Replit & Render) → lỗi IP block là expected
  - YouTube: hoạt động hoàn toàn, không cần cookies trên Replit hiện tại

YOUTUBE_COOKIES Secret (nếu cần khi YouTube bắt đầu block):
  1. Cài extension "Get cookies.txt LOCALLY" trên Chrome
  2. Đăng nhập YouTube → export cookies.txt
  3. Set Secret YOUTUBE_COOKIES = nội dung file (plain text hoặc base64)
  4. Restart API Server

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[07/04/2026 session 7] SETUP — Migration sang Replit environment mới
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → 730 packages (done in 35.5s)
  2. npm install -g node-gyp --prefix=/tmp/gyp-install
  3. Rebuild better-sqlite3 native binary:
     - cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     - /tmp/gyp-install/bin/node-gyp configure && build
     → better_sqlite3.node ✅
  4. Restart workflow "Start application" (Portfolio port 21113) → RUNNING ✅
  5. Tạo + start workflow "packages/api-server: API Server":
     - Lệnh: PORT=8080 pnpm --filter @workspace/api-server run dev
     → RUNNING ✅

Kết quả:
  - Portfolio (port 21113): RUNNING ✅
  - API Server (port 8080): RUNNING ✅
  - WebSocket /ws và /ws-browser: sẵn sàng ✅
  - SQLite DB initialized ✅
  - yt-dlp binary: ready ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt (cần set trong Secrets)

Lưu ý mỗi khi env reset:
  1. pnpm install --ignore-scripts
  2. npm install -g node-gyp --prefix=/tmp/gyp-install
  3. cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     /tmp/gyp-install/bin/node-gyp configure && /tmp/gyp-install/bin/node-gyp build

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 6] FEAT — Thêm WiFi hotspot thuyết trình
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: firmware/NexoraGarden/NexoraGarden.ino
GitHub commit: 2457774b05033dba9673716568aa8dc87d5b1ac0

Thêm WiFi "phantrongkhangg" / "26042012khang" vào đầu danh sách (ưu tiên cao nhất)
NUM_WIFI: 3 → 4

ESP32 cần nạp firmware: CÓ


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 6] FIX FIRMWARE — 7 lỗi ESP32 (quét toàn bộ)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: firmware/NexoraGarden/NexoraGarden.ino
GitHub commit: 68f764a48e66f9f82a7aef8b060a8c40a11fc6d1

Fix 1 — handlePump() dùng map() sai:
  - Đổi từ map(raw, SOIL_RAW_DRY, SOIL_RAW_WET, 0, 100) sang multiPointMap(raw)
  - liveSoil giờ nhất quán với soilPercent hiển thị → bơm dừng đúng thời điểm

Fix 2 — Admin OFF → bơm tự bật ngay:
  - Thêm pumpLocked = true + cooldown 10 phút sau khi tắt admin mode
  - Tránh auto-pump trigger ngay khi đất còn khô

Fix 3 — Lửa không gửi server trong 3 giây đầu:
  - Đổi fireNow = fireActive (thay vì fireActive && fireAlerted)
  - Dashboard nhận fire=true ngay lập tức; 3s debounce chỉ cho notify Telegram

Fix 4 — WebSocket heartbeat 1 retry (dễ disconnect loop):
  - Đổi enableHeartbeat(15000, 8000, 1) → enableHeartbeat(15000, 8000, 2)
  - 2 retry: chịu được 1 lần pong chậm, tránh reconnect loop
  - Vẫn detect server chết sau ~46s (watchdog 3 phút vẫn hoạt động)

Fix 5 — getMedian() chia cho 0 nếu n < 5:
  - Thêm guard: if (n < 5) return arr[n/2];

Fix 7 — Biến lastCal global dư thừa khi CALIBRATE_MODE:
  - Xóa global unsigned long lastCal = 0; (chỉ giữ static local trong printCalibrate)

Fix 8 — liveSoil dùng multiPointMap:
  - Đã xử lý cùng Fix 1

ESP32 cần nạp firmware: CÓ


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 6] SETUP — Khởi động lại Replit environment
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → 730 packages (reused từ cache, done in 25s)
  2. Rebuild better-sqlite3 native binary:
     - npm install -g node-gyp --prefix=/tmp/gyp-install
     - node-gyp configure + build tại better-sqlite3
     → better_sqlite3.node ✅
  3. Restart workflow "packages/api-server: API Server" → RUNNING ✅
  4. Restart workflow "Start application" (Portfolio) → RUNNING ✅

Kết quả:
  - API Server (port 8080): RUNNING ✅
  - WebSocket /ws và /ws-browser: sẵn sàng ✅
  - SQLite DB initialized ✅
  - Portfolio: RUNNING ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt

Lưu ý mỗi khi env reset:
  1. pnpm install --ignore-scripts
  2. cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
     /tmp/gyp-install/bin/node-gyp configure && /tmp/gyp-install/bin/node-gyp build

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 5] FIX — YtDownloader: nhiều fix liên tiếp (cobalt → yt-dlp)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts

Fix 1 — cobalt.tools JWT auth:
  - Bỏ cobalt.tools, dùng yt-dlp qua child_process.execFile
  - findExistingBin() tìm binary theo nhiều path

Fix 2 — ENOENT: binary không tìm thấy trên Render:
  - Lazy init (không throw tại module load)
  - ensureYtDlpBin(): auto-download từ github.com/releases/latest/download/yt-dlp
  - getYtDlpBin(): cache Promise (concurrent-safe)

Fix 3 — GitHub API 403 rate limit:
  - Bỏ gọi api.github.com (bị 403 từ datacenter IP)
  - Download trực tiếp: github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp

Fix 4 — YouTube bot check + deprecated flags:
  - Bỏ --no-call-home, --youtube-skip-dash-manifest (deprecated)
  - Thêm --extractor-args youtube:player_client=android,web
    → Android client bypass bot check trên datacenter IP
  - Pass URL sạch watch?v=ID (bỏ ?si= tracking params)
  - Test OK local: KARAOKE TIẾNG CHUÔNG VÀ NGỌN CỜ ✅

Kết quả cuối:
  - 360p: CDN URL trực tiếp từ YouTube ✅
  - 720p+: stream qua server (fallback)

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 5] FIX — YtDownloader: cobalt.tools JWT auth, fix dùng child_process
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts
       apps/portfolio/src/pages/tools/YtDownloader.tsx

Vấn đề:
  - cobalt.tools thêm JWT auth bắt buộc → không còn free public API
  - Code cũ dùng cobalt.tools API → 401 Unauthorized trên production
  - yt-dlp-exec library bị esbuild bundle mất __dirname → ENOENT binary

Đã sửa:
  - Bỏ hoàn toàn cobalt.tools
  - Dùng child_process.execFile trực tiếp với yt-dlp binary (tránh esbuild path issue)
  - findYtDlpBin(): tìm binary theo thứ tự: env YOUTUBE_DL_PATH → createRequire resolve → fallback path cứng
  - GET /api/yt/info: yt-dlp --dump-single-json → lấy combined formats (video+audio)
    - 360p (itag 18): CDN URL trực tiếp → frontend dùng thẳng ✅
    - 720p/1080p: adaptive (không có combined) → fallback /api/yt/stream
  - GET /api/yt/stream: stream video qua server dùng yt-dlp -o - | pipe to response
  - Frontend: handleDownload đơn giản hơn — dùng fmt.url trực tiếp, không cần fetch trung gian

Kết quả test:
  - /api/yt/info Rick Astley: title ✅ thumbnail ✅ channel ✅ duration ✅
  - 360p CDN URL: HTTP 200 video/mp4 11MB ✅
  - 720p+: stream endpoint (CDN từ datacenter IP có thể bị block)

Lưu ý:
  - yt-dlp binary ở: node_modules/.pnpm/yt-dlp-exec@1.0.2/node_modules/yt-dlp-exec/bin/yt-dlp
  - Mỗi khi env reset: chạy postinstall để re-download binary:
    node node_modules/.pnpm/yt-dlp-exec@1.0.2/node_modules/yt-dlp-exec/scripts/postinstall.js

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 4] FIX — YtDownloader: bỏ hoàn toàn Invidious, dùng yt-dlp-exec
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts
Package mới: yt-dlp-exec@1.0.2 (tự download binary yt-dlp v2026.03.17)
pnpm-workspace.yaml: thêm yt-dlp-exec vào onlyBuiltDependencies

Vấn đề:
  - Invidious trả HTML/Cloudflare từ datacenter IP (Render/Replit)
  - ytdl-core và youtubei.js: không decipher được cipher → url=false cho format 720p+
  - Chỉ lấy được 360p itag=18 nhưng url=false → không dùng được

Giải pháp: yt-dlp-exec
  - yt-dlp v2026.03.17 decipher thành công YouTube CDN URLs
  - Test: Rick Astley → 360p mp4 hasUrl=true, HTTP 200 từ CDN
  - API /api/yt/info trả về: title, thumbnail, duration, channel, formats[]
  - Formats: combined (video+audio) với URL trực tiếp → frontend download không qua server
  - Fallback: /api/yt/download endpoint server-side stream nếu không có combined format

Kết quả test local:
  - title: "Rick Astley - Never Gonna Give You Up" ✅
  - channel: "Rick Astley" ✅
  - thumbnail URL ✅
  - formats: [{q:"360p", ext:"mp4", hasUrl:true}] ✅
  - CDN URL: HTTP 200, Content-Type: video/mp4 ✅

Lưu ý Render production:
  - yt-dlp-exec tự download binary trong postinstall → hoạt động trên Render
  - 360p: không cần ffmpeg (combined format)
  - 720p+: cần ffmpeg (adaptive streams cần mux) — chưa test trên Render

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 3] FIX — YtDownloader: Invidious instances chết, cập nhật list mới
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: packages/api-server/src/routes/youtube.ts

Vấn đề:
  - Các Invidious instance trong backend đều chết (404, 401, 500, timeout)
  - Test thực từ server: chỉ còn 4 instance cho HTTP 200:
    protokolla.fi, iv.ggtyler.dev, slipfox.xyz, materialio.us

Đã sửa:
  - Đặt 4 instance đang sống lên đầu danh sách
  - Thêm redirect: "follow" để xử lý 301 redirect
  - Giữ lại các instance cũ ở cuối list (dự phòng nếu instance trên chết)

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 3] FIX — YtDownloader: CORS block khi fetch Invidious
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: apps/portfolio/src/pages/tools/YtDownloader.tsx

Vấn đề:
  - Browser fetch Invidious trực tiếp → "Failed to fetch" (CORS block)
  - Invidious instance không cho phép cross-origin request từ nexorax.cloud
  - Dù thêm bao nhiêu instance cũng vô dụng vì CORS block ở browser

Đã sửa:
  - Xóa toàn bộ INVIDIOUS_INSTANCES + fetchVideoInfo() browser-side
  - Thay bằng gọi backend API: /api/yt/info?url=...
  - Backend đã có sẵn route này (packages/api-server/src/routes/youtube.ts)
  - Backend fetch Invidious server-side → không bị CORS
  - Production: portfolio + API cùng domain nexorax.cloud → same-origin

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 3] SETUP — Khởi động lại môi trường Replit
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → cài 712 packages
  2. Rebuild better-sqlite3 native bindings (node-gyp build)
     → better_sqlite3.node 2.2MB ✅
  3. Khởi động workflow "Start application" (Portfolio port 21113) ✅
  4. Tạo workflow "API Server":
     - Lệnh: PORT=8080 pnpm --filter @workspace/api-server run dev
     - Port 8080, outputType: console ✅

Kết quả:
  - Portfolio chạy tại port 21113 ✅
  - API Server chạy tại port 8080 ✅
  - WebSocket /ws và /ws-browser sẵn sàng ✅
  - SQLite DB initialized ✅
  - TELEGRAM_TOKEN chưa set → Bot Telegram tắt (cần set trong Secrets)

Lưu ý rebuild better-sqlite3 mỗi khi env reset:
  cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
  /tmp/gyp-install/bin/node-gyp configure && /tmp/gyp-install/bin/node-gyp build

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 2] FIX — YtDownloader: Invidious bị block server IP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: apps/portfolio/src/pages/tools/YtDownloader.tsx

Vấn đề:
  - Tất cả Invidious instance đều báo lỗi khi tìm kiếm
  - Nguyên nhân gốc: backend (Render server = datacenter IP) gọi Invidious
    → Invidious block datacenter IP (chỉ trả captcha/fingerprint HTML)
  - Dù thêm bao nhiêu instance cũng vô dụng nếu gọi từ server

Đã sửa:
  - Xóa gọi backend `/api/yt/info` hoàn toàn
  - Frontend tự fetch Invidious API trực tiếp từ browser (IP người dùng)
  - Browser IP (4G/nhà) không bị Invidious block
  - Thêm 9 instances vào danh sách, thử lần lượt
  - Hiển thị hint "Đang thử các server..." khi loading

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 2] FIX — YtDownloader màn đen khi tìm kiếm
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: apps/portfolio/src/pages/tools/YtDownloader.tsx

Vấn đề:
  - Nhập link → nhấn Tìm → màn hình đen
  - Nguyên nhân: Loader2 được dùng ở JSX nhưng KHÔNG được import
  - Khi loading=true → render <Loader2> → crash → blank screen

Đã sửa:
  - Thêm Loader2 vào import từ lucide-react

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026 session 2] FIX — better-sqlite3 native binary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - Môi trường bị reset → node_modules mất
  - pnpm install --ignore-scripts không build native .node file
  - pnpm install lần 2 bỏ qua vì "already up to date"
  - node-gyp không có sẵn trong PATH

Đã làm:
  1. pnpm install --ignore-scripts → cài 712 packages
  2. installSystemDependencies: gcc, gnumake, python3, nodejs_24 (Nix)
  3. npm install -g node-gyp --prefix=/tmp/gyp-install → có node-gyp
  4. node-gyp configure + build tại thư mục better-sqlite3
     → Compile thành công: build/Release/better_sqlite3.node (2.27MB)
  5. Restart workflow "API Server" → RUNNING ✅

Kết quả:
  - "Start application" (Portfolio) RUNNING port 21113 ✅
  - "API Server" RUNNING port 8080 ✅
  - WebSocket /ws + /ws-browser ready ✅
  - SQLite DB initialized ✅
  - TELEGRAM_TOKEN chưa set (cần thêm vào Secrets nếu dùng bot)

Lưu ý:
  - Mỗi khi môi trường reset, cần rebuild better-sqlite3:
    cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3
    /tmp/gyp-install/bin/node-gyp configure && /tmp/gyp-install/bin/node-gyp build
  - Workflows từ attached_assets/ bị FAILED (code cũ, bỏ qua)

ESP32 cần nạp firmware: KHÔNG


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026] SETUP REPLIT — Import project vào môi trường mới
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vấn đề:
  - Node modules chưa được cài (pnpm install chưa chạy)
  - better-sqlite3 cần native build (C++ addon)
  - Thiếu build tools: gcc, make

Đã làm:
  1. pnpm install --ignore-scripts  → cài tất cả deps
  2. installSystemDependencies: gcc, gnumake, python3 (qua Nix)
  3. pnpm rebuild  → build better-sqlite3 native bindings
  4. Restart workflow "packages/api-server" và "Start application"

Kết quả:
  - API Server chạy tại PORT 8080 ✅
  - Portfolio chạy tại PORT 21113 ✅
  - SQLite DB initialized ✅
  - WebSocket sẵn sàng ✅


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026] FIX TFT — Màn hình mất đồng bộ khi bật/tắt bơm
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: firmware/NexoraGarden/NexoraGarden.ino
GitHub commit: e0c39036b92fe24a0938c847e64ca82c5b142a2f

Vấn đề:
  - Bật/tắt bơm → TFT clear trắng, không vẽ lại
  - Gốc rễ: relay click → SPI noise → TFT ST7789 mất đồng bộ

Đã thêm:
  - Biến: unsigned long tftReinitAt = 0;
  - setPump(): thay tftForceRedraw=true bằng tftReinitAt=millis()+150
  - manageTFT():
      + Khi tftReinitAt đến: tft.init() + fillScreen + redraw
      + Watchdog 2s: nếu không vẽ > 2s → tự reinit
  - setup(): tftLastDraw = millis() (tránh watchdog trigger ngay)

Kết quả: TFT tự phục hồi sau mỗi relay click ✅


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026] FIX #1+2 — Cảm biến mưa báo nhầm khi bơm bật
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: firmware/NexoraGarden/NexoraGarden.ino
       apps/nexora-garden/server/websocket.ts
GitHub commit: (xem commit mới nhất)

Vấn đề:
  - Bơm bật → nước bắn vào cảm biến mưa → gửi rain=true liên tục
  - handleFire() có guard "if pumpState return" → lửa bị tắt khi bơm (SAI)

Đã sửa trong firmware:
  - handleFire(): BỎ guard pumpState → lửa luôn active kể cả khi bơm
  - handleRain(): THÊM guard "if (pumpState) { rainAlerted=false; return; }"
  - shouldSendData(): rain = pumpState ? false : digitalRead(RAIN_PIN)
  - sendSensorData(): rain = pumpState ? false : digitalRead(RAIN_PIN)

Kết quả: khi bơm bật, chỉ soil + fire hoạt động, rain bị bỏ qua ✅


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026] FIX #3 — Cảm biến nước raw=0 → hiển thị dưới 15%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: firmware/NexoraGarden/NexoraGarden.ino
       apps/nexora-garden/server/websocket.ts
GitHub commit: (xem commit mới nhất)

Vấn đề:
  - Cảm biến mức nước bị thiếu khúc đáy vật lý
  - Khi nước xuống tới đó: raw = 0 → hiển thị 0% hoặc lỗi (SAI)
  - Cần: raw=0 → báo thấp (dưới 15%) thay vì 0%/lỗi

Đã sửa trong firmware (updateWater):
  - if (raw == 0): mapped = 10  (thay vì 0)
  - Ngưỡng cảnh báo: waterPercent <= 15 (thay vì < 10)
  - Tin nhắn: "duoi 15%" (thay vì "duoi 10%")

Đã sửa TFT (drawMainScreen):
  - } else if (waterPercent <= 15) {  → hiển thị "THAP X%"

Đã sửa server (websocket.ts):
  - if (water === 0) water = 10;  (trước khi xử lý spike)
  - Ngưỡng cảnh báo: water <= 15 (thay vì <= 10)

Kết quả: raw=0 → TFT hiển thị "THAP 10%", server log cảnh báo ✅


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
THÔNG TIN QUAN TRỌNG ĐỂ TIẾP TỤC
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GitHub repo   : khang26042012/Nexora (branch main)
GitHub token  : [xem replit.md — không lưu vào file push GitHub]
Push method   : Git Tree API (1 blob → 1 tree → 1 commit → update ref)
                KHÔNG dùng Contents API (nhiều commit = nhiều Render deploy)

Files quan trọng:
  firmware/NexoraGarden/NexoraGarden.ino  — ESP32 code
  apps/nexora-garden/server/websocket.ts  — WebSocket + sensor logic
  apps/nexora-garden/server/telegram.ts   — Bot Telegram
  apps/nexora-garden/server/db.ts         — SQLite schema + queries
  apps/nexora-garden/server/routes.ts     — REST API endpoints

Workflows đang chạy:
  "Start application"              → Portfolio (port 21113)
  "packages/api-server: API Server" → API + WebSocket (port 8080)

Sau khi sửa server code: restart workflow "packages/api-server"
Sau khi sửa firmware: push GitHub → Khang nạp lại vào ESP32


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026] MIGRATION — Chuyển sang Replit environment mới
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. pnpm install --ignore-scripts → cài toàn bộ 712 packages
  2. installSystemDependencies: gcc, gnumake, python3 (Nix)
  3. pnpm rebuild better-sqlite3 → build native bindings thành công
  4. Tạo workflow "API Server":
     - Lệnh: PORT=8080 pnpm --filter @workspace/api-server run dev
     - Port 8080, outputType: console
  5. Restart workflow "Start application" (Portfolio port 21113)

Kết quả:
  - Portfolio chạy tại port 21113 ✅
  - API Server chạy tại port 8080 ✅
  - SQLite DB initialized ✅
  - WebSocket /ws và /ws-browser sẵn sàng ✅
  - TELEGRAM_TOKEN chưa set → Telegram tắt (cần thêm vào Secrets nếu dùng)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[06/04/2026] YouTube Downloader — Làm logic tải thực sự
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Đã làm:
  1. Cài yt-dlp + ffmpeg (Nix system packages)
  2. Tạo API routes:
     - GET /api/yt/info?url=...   → fetch title, thumbnail, formats
     - GET /api/yt/download?url=...&itag=...&title=...  → stream MP4
  3. Tạo trang frontend: apps/portfolio/src/pages/tools/YtDownloader.tsx
     - Input URL + nút Tìm
     - Hiển thị thumbnail, tên video, kênh, thời lượng
     - Chọn chất lượng (1080p / 720p / 480p / 360p)
     - Nút Tải xuống → download file về máy
  4. Thêm route /tool/yt-downloader vào App.tsx
  5. Cập nhật Tool.tsx: click card → navigate tới trang downloader

Files đã tạo/sửa:
  packages/api-server/src/routes/youtube.ts   (MỚI)
  packages/api-server/src/routes/index.ts     (thêm /yt route)
  apps/portfolio/src/pages/tools/YtDownloader.tsx  (MỚI)
  apps/portfolio/src/pages/Tool.tsx           (thêm route + onClick)
  apps/portfolio/src/App.tsx                  (thêm route /tool/yt-downloader)

Kết quả test:
  API /api/yt/info → trả về 4 format: 1080p, 720p, 480p, 360p ✅
  yt-dlp merge video + audio → xuất file MP4 ✅

ESP32 cần nạp firmware: KHÔNG

================================================================

---
[2026-04-07] Redesign Hero hoàn toàn theo ảnh tham khảo:
- ThreeScene.tsx v4: Stars 3 layer (8000+), NebulaClouds gaussian (5 clusters/màu), AuroraRibbons sine-wave, FloatingOrbs 3 orbs, CameraController parallax - FIX useMemo trong map
- Home.tsx Hero: Ambient CSS glow orbs (4 orbs), Mountain silhouette SVG 2 lớp có glow line, Crystal formations SVG 3 cụm (cyan/blue/purple) với SVG filter glow, Floor gradient, Text glow shadows, Button shadow nổi bật
- Page background: linear-gradient tím sâu thay solid
