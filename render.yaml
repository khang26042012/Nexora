services:
  - type: web
    name: nexorax-portfolio
    runtime: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter @workspace/portfolio run build
    staticPublishPath: artifacts/portfolio/dist/public
    envVars:
      - key: NODE_VERSION
        value: 24
      - key: BASE_PATH
        value: /
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
